import time
import random


'''
Мальцев Дмитрий, АСУ8-24-1м

Задача 1. 
    Реализовать один из вариантов сбалансированного дерева поиска (можно 
выбрать один любой из перечисленных ниже): 
- АВЛ-дерево; 
- красно-черное дерево; 
- АА-дерево; 
- 2-3 дерево; 
- Splay-дерево. 
Напишите в комментариях в программе, какой из вариантов реализован. 
Для проверки производительности написать основную программу, в которой: 
- создать список (вектор) и реализованное дерево; 
- сгенерировать случайным образом 10^5 целых чисел в диапазоне от -10^6 до 10^6, 
каждое из них записать в список и в дерево; 
- сгенерировать еще 105 целых чисел в том же диапазоне, каждое проверить на наличие  
в списке и в дереве, посчитать общее количество (сколько из сгенерированных во второй 
раз чисел есть в списке и в дереве); 

    Примечание. Выполнение этого цикла должно занимать ощутимое, но разумное время 
(несколько секунд или десятков секунд). Если цикл выполняется быстро – попробуйте 
увеличить количество чисел до 3*10^5 или еще больше. Если не удается дождаться  
результата (выполняется больше минуты) – попробуйте уменьшить количество чисел. 
- сравнить полученные результаты (очевидно, раз список и дерево заполнены одними 
и теми же числами, то ответы должны совпасть); 
- убрать из цикла проверку на наличие в списке (оставить только проверку наличия в 
дереве) и замерить, как изменилось время работы. 
    Замеры времени рекомендуется реализовать точно, с использованием 
соответствующих функций языка. В любом случае в примечаниях к программе напишите 
результаты сравнения времени.
'''

# АВЛ-дерево








class AVLNode:
    def __init__(self, key):
        self.key = key      # Значение узла (ключ)
        self.left = None    # Левый потомок
        self.right = None   # Правый потомок
        self.height = 1     # Высота узла (нужна для балансировки)

class AVLTree:
    def get_height(self, node):
        return node.height if node else 0
    
    def get_balance(self, node):
        '''
        Баланс. Для каждого узла вычисляется фактор баланса (𝐹𝐵):
        𝐹𝐵 = высота левого поддерева−высота правого поддерева
        Если 𝐹𝐵 = -1, 0, 1 — дерево сбалансировано.
        Если 𝐹𝐵 < -1 или 𝐹𝐵 > 1 — дерево несбалансированное, нужно применять вращения.
        '''
        return self.get_height(node.left) - self.get_height(node.right) if node else 0

    def rotate_right(self, y):
        x = y.left
        T = x.right
        x.right = y
        y.left = T
        # Обновляем высоты предков
        y.height = max(self.get_height(y.left), self.get_height(y.right)) + 1
        x.height = max(self.get_height(x.left), self.get_height(x.right)) + 1
        return x

    def rotate_left(self, x):
        y = x.right
        T = y.left
        y.left = x
        x.right = T
        # Обновляем высоты предков
        x.height = max(self.get_height(x.left), self.get_height(x.right)) + 1
        y.height = max(self.get_height(y.left), self.get_height(y.right)) + 1
        return y

    # Вставка (O(log N))
    def insert(self, root, key):
        if not root:
            return AVLNode(key)
        elif key < root.key:
            root.left = self.insert(root.left, key)
        else:
            root.right = self.insert(root.right, key)
        
        root.height = max(self.get_height(root.left), self.get_height(root.right)) + 1
        balance = self.get_balance(root)
        '''
        После каждой вставки или удаления баланс дерева проверяется с помощью 
        баланса высот левого и правого поддеревьев. Если баланс нарушен, 
        применяются вращения для его восстановления:

        Чтобы восстановить баланс, применяются четыре типа вращений:
        Правое (RR) — когда высокий дисбаланс справа (FB = -2, правый ребенок выше).
        Левое (LL) — когда высокий дисбаланс слева (FB = +2, левый ребенок выше).
        Лево-правое (LR) — когда новый узел вставлен в правого потомка левого узла.
        Право-левое (RL) — когда новый узел вставлен в левого потомка правого узла.
        '''
        if balance > 1 and key < root.left.key:
            return self.rotate_right(root)
        if balance < -1 and key > root.right.key:
            return self.rotate_left(root)
        if balance > 1 and key > root.left.key:
            root.left = self.rotate_left(root.left)
            return self.rotate_right(root)
        if balance < -1 and key < root.right.key:
            root.right = self.rotate_right(root.right)
            return self.rotate_left(root)
        
        return root
    
    # Поиск (O(log N))
    def search(self, root, key):
        # Работает так же, как в обычном двоичном дереве поиска 
        # — идем влево, если число меньше, и вправо, если больше.
        if not root or root.key == key:
            return root is not None
        elif key < root.key:
            return self.search(root.left, key)
        else:
            return self.search(root.right, key)

# Генерация случайных чисел
N = 10**5                   # количество чисел
мин, макс = -10**6, 10**6   # диапазон значений чисел
случайные_числа = [random.randint(мин, макс) for _ in range(N)]
новые_числа = [random.randint(мин, макс) for _ in range(N)]

# Тестирование списка
список_чисел  = []
начало1 = time.time()
список_чисел.extend(случайные_числа)
конец1 = time.time() - начало1

# Измеряем время поиска в списке
начало2 = time.time()
совпадения_список = sum(1 for число in новые_числа if число in список_чисел)
конец2 = time.time() - начало2


# Тестирование АВЛ-дерева
avl_дерево = AVLTree()
дерево = None
начало3 = time.time()
for число in случайные_числа:
    дерево = avl_дерево.insert(дерево, число)
конец3 = time.time() - начало3

# Измеряем время поиска в АВЛ-дереве
начало4 = time.time()
совпадения_дерево = sum(1 for число in новые_числа if avl_дерево.search(дерево, число))
конец4 = time.time() - начало4


# Повторный тест без списка
# *Убираем проверку списка и замеряем время только для дерева
начало5 = time.time()
только_совпадения_дерево = sum(1 for число in новые_числа if avl_дерево.search(дерево, число))
конец5 = time.time() - начало5

print('[ Тестирование списка ]')
print(f"Время вставки в список: {конец1:.4f} сек")
print(f"Время поиска в списке: {конец2:.4f} сек")
print(f"Совпадений в списке: {совпадения_список}")
print('\n[ Тестирование АВЛ-дерева ]')
print(f"Время вставки в АВЛ-дерево: {конец3:.4f} сек")
print(f"Время поиска в АВЛ-дереве: {конец4:.4f} сек")
print(f"Совпадений в АВЛ-дереве: {совпадения_дерево}")
print(f"Время поиска в АВЛ-дереве (без списка): {конец5:.4f} сек")

# Сравнение результатов
assert совпадения_список == совпадения_дерево, 'Ошибка: данные не совпадают!'
print('Результаты совпадают!')

'''
[ Тестирование списка ]
Время вставки в список: 0.0000 сек
Время поиска в списке: 30.9122 сек
Совпадений в списке: 4987

[ Тестирование АВЛ-дерева ]
Время вставки в АВЛ-дерево: 0.5260 сек
Время поиска в АВЛ-дереве: 0.1110 сек
Совпадений в АВЛ-дереве: 4987
Время поиска в АВЛ-дереве (без списка): 0.1080 сек
Результаты совпадают!
'''